<!DOCTYPE html>
    <head>
        <title>Symfony 3 CRUD Application</title>
        <link rel="stylesheet" href="{{ absolute_url(asset('css/bootstrap.min.css')) }}" />
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="#">Symfony 3 CRUD ApplicationNavbar</a>
        </nav>